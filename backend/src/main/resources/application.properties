spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/auto_ocasion?createDatabaseIfNotExist=true&serverTimezone=Europe/Madrid}
spring.datasource.username=${DB_USER:autoocasion}
spring.datasource.password=${DB_PASSWORD:autoocasion}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.servlet.multipart.max-file-size=10MB
app.admin.email=${ADMIN_EMAIL:admin@autoocasion.local}
app.admin.password=${ADMIN_PASSWORD:Admin123!SantAdria}
app.upload-dir=${UPLOAD_DIR:uploads}
app.whatsapp.phone=${WHATSAPP_PHONE:34678773271}
